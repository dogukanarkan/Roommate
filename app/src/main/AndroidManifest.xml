<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lykia.roommate">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".LoginActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".VerificationPasswordActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".OwnProfileActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".AnimalActivity" />
        <activity android:name=".MessageActivity" />
        <activity android:name=".AllMessageActivity" />
        <activity android:name=".RehomingActivity" />
        <activity android:name=".BriefArticleActivity" />
        <activity android:name=".DetailArticleActivity" />
        <activity android:name=".AddArticleActivity" />
    </application>

</manifest>